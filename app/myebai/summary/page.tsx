// Import necessary libraries and external components
// Import internal components
// Import styles
// Fetch or get data

import MainLayout from "@/app/layouts/main";

const Page = () => {
  return (
    <MainLayout>
      <p className="h-100">Đang bận vcl</p>
    </MainLayout>
  );
};

export default Page;
